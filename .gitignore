__pycache__
.ipynb_checkpoints

# packaging / editable install
*.egg-info/
build/
dist/

# experiment prediction dumps — one row per (date, horizon), regenerated by
# `make experiments`. The scored summaries and run logs are tracked; these are not.
output/exp1_predictions.csv
output/exp2_predictions.csv
